"use client";

import { AppSidebar } from "./app-sidebar";

/**
 * MainSidebar is aliased to AppSidebar to maintain single unified sidebar implementation
 * and preserve backward compatibility across all imports.
 */
export const MainSidebar = AppSidebar;
